[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mco"
version = "0.11.0"
description = "Neutral multi-provider CLI orchestrator for agent-style run/review workflows."
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "mco maintainers" }
]
license = {text = "MIT"}
keywords = ["cli", "orchestrator", "ai", "agents", "review"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Environment :: Console",
  "Operating System :: OS Independent",
  "Topic :: Software Development :: Quality Assurance",
  "Topic :: Software Development :: Testing",
]
dependencies = []

[project.optional-dependencies]
memory = ["mcp>=1.29,<2"]

[project.urls]
Homepage = "https://github.com/mco-org/mco"
Repository = "https://github.com/mco-org/mco"
Issues = "https://github.com/mco-org/mco/issues"

[project.scripts]
mco = "runtime.hyper_cli:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["runtime*", "skills"]

[tool.setuptools.package-data]
runtime = ["data/*.json"]
skills = ["mco-cli/*.md", "mco-cli/references/*.md"]
