name: Gate

on:
  pull_request:
  push:
  workflow_dispatch:
    inputs:
      run_capability_probes:
        description: "Run capability probes (requires self-hosted macOS runner with configured CLIs)"
        required: false
        default: false
        type: boolean

jobs:
  runtime-gate:
    name: Runtime G/H gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: sudo apt-get update && sudo apt-get install -y jq ripgrep

      - name: Install MCP integration dependency
        run: python3 -m pip install -e '.[memory]'

      - name: Run runtime gate tests
        run: ./scripts/run_runtime_gate_tests.sh

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Node dependencies
        run: npm ci

      - name: Run Node tests
        run: npm test

      - name: Validate bundled skill format
        run: python3 scripts/check_skill_format.py skills/mco-cli

      - name: Validate package/runtime version alignment
        run: python3 scripts/check_package_version.py

      - name: Run npm packaging smoke
        run: ./scripts/run_npm_packaging_smoke.sh

      - name: Collect CI artifact index
        if: always()
        run: |
          DATE_STR="$(date +%F)"
          python3 ./scripts/collect_ci_artifacts.py \
            --report-root "reports/adapter-contract/$DATE_STR" \
            --workflow "gate" \
            --run-id "${{ github.run_id }}"
          if [ -f "reports/adapter-contract/$DATE_STR/ci-artifact-index.md" ]; then
            cat "reports/adapter-contract/$DATE_STR/ci-artifact-index.md" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload runtime gate artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: runtime-gate-${{ github.run_id }}
          path: reports/adapter-contract/**
          if-no-files-found: ignore

  capability-probes:
    name: Capability probes (manual)
    if: github.event_name == 'workflow_dispatch' && inputs.run_capability_probes == true
    runs-on:
      - self-hosted
      - macOS
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run capability probes
        run: ./scripts/run_capability_probes.sh

      - name: Upload capability probe artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: capability-probes-${{ github.run_id }}
          path: docs/probes/**
          if-no-files-found: ignore

  packaging-smoke-macos:
    name: Packaging smoke (macOS)
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Node dependencies
        run: npm ci

      - name: Run packaging smoke
        run: ./scripts/run_npm_packaging_smoke.sh

  installer-smoke-windows:
    name: Installer smoke (Windows)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies and run contracts
        run: |
          npm ci
          npm test
          python -m unittest tests.test_platform_helpers tests.test_acp_transport
          python scripts/check_skill_format.py skills/mco-cli
          python scripts/check_package_version.py

      - name: Pack and install npm artifact
        shell: pwsh
        run: |
          $tarball = (npm pack --silent).Trim()
          $installDir = Join-Path $env:RUNNER_TEMP "mco-npm-install"
          npm install $tarball --prefix $installDir --no-audit --no-fund
          $mco = Join-Path $installDir "node_modules/.bin/mco.cmd"
          & $mco --help | Out-Null
          & $mco --version | Out-Null
          & $mco skills read | Out-Null
          & $mco install --agent codex --dry-run --json | Out-Null
