# CLAUDE.md

## MCO Hyper Development Mode（開發期間）

本儲存庫是 upstream MCO 的主動開發 fork。進行 MCO Hyper 相關開發時，必須遵守以下規則：

1. 將 [`docs/MCO-HYPER-ROADMAP.md`](./docs/MCO-HYPER-ROADMAP.md) 視為 fork-specific 修改的權威目標規格。
2. 現有 `README.md`、既有文件與目前 runtime 行為代表 upstream baseline；除非 Roadmap 明確要求變更，否則應維持相容，不得把規劃中能力誤寫成已完成能力。
3. 依 Roadmap 的 milestone 與 PR 邊界漸進實作；處理 P0 時不得順手實作 P1/P2，除非是已明確記錄且不可避免的依賴。
4. External Lead coding agent 必須維持 Orchestrator 角色；MCO Hyper 是可由外部 Lead 透過 CLI 呼叫的 orchestration runtime，不建立取代 Lead 的自有 reasoning loop。
5. Native Provider CLI 與 native session 行為維持權威來源；Provider 支援 exact native resume 時，優先使用 native session continuity。
6. 不得引入 semantic routing、LLM-based worker selection、semantic result normalization、強制 findings/verdict schema、LLM inference proxy/router，或 Hive 的 PTY/team-runtime 架構。
7. Worker 的自然語言回答維持 opaque；不得因新增 session/profile 功能而改變既有 raw-answer contract。
8. 若實作需求與 Roadmap、upstream baseline 或現有測試產生架構衝突，必須明確提出並記錄，不得自行默默改寫產品邊界。
9. 本區塊是開發期間的 modification contract。當 Roadmap 的目標能力完成後，應將已成為永久架構原則的內容收斂回正式 contributor/runtime 規範，並移除純開發期指示。

---

此文件之所以存在，是因為 LLM 在編寫程式碼時會犯**可預測的錯誤**。不是隨機的錯誤，而是同樣的錯誤反覆出現。我見過足夠多次，因此把它們記錄下來。

這些**不是建議，而是規則**。遵守它們，你將產出無需重寫的程式碼；忽視它們，你將產出看起來 impressive 但在正式環境中會出問題的程式碼。

## 1. 先閱讀再編寫

LLM 產生糟糕程式碼的最大單一來源，是**在編寫新程式碼之前沒有閱讀現有的程式碼庫**。你看到一個任務，就根據訓練資料中的模式開始生成程式碼，這幾乎總是錯誤的。

在編寫任何內容之前：

- 閱讀你即將修改的檔案。不是瀏覽，而是**認真閱讀**。
- 查看專案中其他類似功能的實作方式。如果有 API 路由的模式，就遵循該模式。如果已有工具函式能完成你所需工作的一半，就使用它。
- 檢查檔案頂部的 import 陳述式，它們會告訴你這個專案實際使用了哪些函式庫。不要在專案到處使用 fetch 的地方引入 axios；不要在專案使用原生方法的地方引入 lodash。
- 查看測試檔案，它們會告訴你預期的行為是什麼，而不是你認為它應該是什麼。

這裡的失敗模式很明顯：你生成了「正確」的程式碼，但它與所在的程式碼庫完全格格不入。它能執行，但看起來像是另一個人寫的（因為確實是另一個實體寫的）。然後人類要麼重寫它以符合專案風格，要麼永遠忍受不一致性。兩者都很糟糕。

如果你不確定專案中某件事的做法，就直接說出來。「我在程式碼庫中沒有看到 X 的模式，應該遵循 Y 中的做法還是採用不同的方式？」總是比猜測更好。

## 2. 先思考再編碼

在弄清楚你到底要做什麼之前，不要開始編寫程式碼。這聽起來顯而易見，但卻是最常見的失敗模式。

實際表現如下：

**陳述你的假設。** 如果使用者說「新增認證」，這可能意味著 session cookies、JWT、OAuth、basic auth 或其他五種方式。不要默默選擇一種。要說：「我假設你想要基於 JWT 的認證，帶 refresh token，並儲存在 httpOnly cookies 中。如果需要其他方式，請告訴我。」如果你錯了，只損失 10 秒；如果你默默猜錯，就損失一小時。

**說明權衡。** 幾乎每種實作選擇都有權衡。如果你新增快取，就說：「這會用記憶體換取速度，並引入快取失效的問題，我們現在需要考慮。」使用者可能會說「其實我不想增加這個複雜度。」最好在寫 200 行程式碼之前就知道。

**如果存在多種方法，簡要呈現它們。** 不要五種，兩到三種即可，並給出推薦。「有兩種方法。方案 A 更簡單，但不處理邊緣情況 X。方案 B 處理所有情況，但會增加對 Z 的依賴。除非你預期 X 真的會發生，否則我推薦 A。」

**如果有困惑，就停下來。** 不要用聽起來合理的程式碼來填補困惑。在不理解需求時生成程式碼，結果是程式碼能通過隨意審查，但在關鍵時刻會失敗。直接說出困惑之處並提問。

## 3. 簡潔性

編寫**解決問題的最小程式碼量**。不是理論上能解決問題的程式碼，而是**目前真正解決這個具體問題**的最小程式碼量。

過度設計的本能很強，要抵抗它。以下是實際中的過度設計表現：

**過早抽象。** 你只需要寄送一種類型的郵件，卻寫了一個 EmailService 類別，帶策略模式支援多種提供商、模板引擎和重試策略。而使用者想要的只是 `sendWelcomeEmail(user)`。先寫那個函式。如果以後需要更多，他們會說的。

（範例對比程式碼已省略，保持原英文範例清晰）

**推測性錯誤處理。** 你給所有東西都套上 try/catch 來處理不可能發生的錯誤。你對來自自己程式碼且上游已驗證的輸入進行驗證。你對永遠不會為 null 的值新增 null 檢查。每行錯誤處理程式碼都是別人需要閱讀和理解的。只處理**實際可能發生**的錯誤。

**不必要的可設定性。** 你把批次處理大小做成參數，把重試次數做成可設定的，為永遠不會變化的東西新增環境變數。設定不是免費的。每個設定選項都是別人需要做出的決定和正確設定的值。在有真實理由之前，先硬編碼。

**無用的靈活性。** 只有一個實作的介面、只有一個子類別的抽象基底類別、只用一種類型實例化的泛型。這些東西有成本（認知開銷、間接層、更多需要導覽的檔案），在第二個實作真正出現之前沒有任何收益。

簡潔性的測試：把你的程式碼展示給不熟悉專案的人看。如果他們問「為什麼這樣抽象？」而你的回答是「以防我們需要……」，那你就過度設計了。「以防我們需要」不是需求，它是對未來的猜測，而對未來的猜測通常是錯的。

## 4. 外科手術式的修改

當編輯現有程式碼時，你的 diff 應該盡可能小。每修改一行程式碼，都可能引入 bug、需要別人審查，並且會永遠出現在 git blame 中。

規則：

**不要碰觸未被要求碰觸的內容。** 如果你在修復函式 A 的 bug，卻發現函式 B 的變數名稱很奇怪，就別管它。如果函式 C 的註解有拼字錯誤，也別管它。如果 import 順序不符合你的喜好，也別管它。你的工作是修復函式 A 的 bug。

**符合現有風格。** 如果檔案使用單引號，就用單引號。如果使用 `snake_case`，就用 `snake_case`。如果檔案沒有分號，就不要新增分號。即使是 2025 年，如果檔案還在用 `var`，那就在你的新增程式碼中使用 `var`，除非使用者要求現代化。檔案內部的一致性勝過個人偏好。

**只清理自己製造的垃圾，不要清理別人的。** 如果你的修改導致某個 import 不再使用，就移除它。如果導致變數不再使用，就移除它。如果導致函式不再使用，就移除它。但僅限於**你的修改導致的**。預先存在的死程式碼不是你的問題，除非有人要求你清理。

**不要重新格式化。** 不要對沒有用 prettier 格式化的檔案執行 prettier。不要把 4 空格縮排改成 2 空格。不要把 import 按字母順序重新排序（如果之前不是這樣的話）。重新格式化會產生巨大的 diff，掩蓋你真正的改動，讓程式碼審查變得痛苦。

測試：查看你的 diff。每行改動都能用與任務的直接關聯來證明嗎？如果有任何一行是因為「我順手就……」而存在，那就撤銷它。

## 5. 驗證

能運作的程式碼與你「認為」能運作的程式碼之間的區別在於**測試**。你應該對此保持偏執。

**修復 bug 時先寫測試。** 在修復任何東西之前，先寫一個能重現 bug 的測試。執行它，看著它失敗。然後修復 bug，再執行測試，看著它通過。這不是可選的，也不是 TDD 教條，而是證明你真正修復了問題的唯一方法。

**在修改前後都執行現有測試。** 如果修改前測試通過，修改後失敗，那你就破壞了什麼。這很明顯。更不明顯的是：如果修改前測試就已經失敗，要明確說出來。不要默默忽略已有的失敗，讓你的改動背鍋。

**不要為了寫測試而寫測試。** 檢查建構函式是否設定屬性的測試毫無價值。檢查你的驗證是否真正拒絕壞輸入的測試才有價值。測試行為，而不是實作。測試有趣的案例，而不是瑣碎的案例。

**如果你無法寫測試，就說明原因。** 有時架構會讓測試變得困難，這本身就是有用的資訊。「我無法輕鬆測試這個，因為資料庫呼叫與商業邏輯緊密耦合」是一個訊號，表明可能需要重構。不要只是跳過測試並心存僥倖。

## 6. 目標驅動的執行

每個任務在開始編寫程式碼之前都應該有明確的成功標準。如果標準模糊，就把它具體化。如果你無法具體化，就提問。

將模糊任務轉化為可驗證的任務：

- 「新增驗證」 → 「拒絕缺少或無效 email 的輸入，回傳 400 狀態碼並附帶說明哪裡出錯的提示訊息，並為兩種情況新增測試」
- 「修復 bug」 → 「編寫重現回報行為的測試，讓測試通過，並驗證現有測試仍通過」
- 「提升效能」 → 「先 profiling，找出瓶頸，修復那個具體問題，然後再次測量」

對於任何超過一步的任務，在執行前先陳述計畫。

這能讓使用者在你浪費時間實作之前發現方法上的錯誤，並迫使你真正思考步驟，而不是直接跳進去邊做邊想。

## 7. 除錯

當出現問題時，不要猜測，要**調查**。

**閱讀錯誤資訊。** 完整閱讀，包括 stack trace。LLM 有個壞習慣，看到錯誤就立即根據錯誤類型生成「修復」，而不讀它實際說了什麼。一個 TypeError 可能有一百種不同含義。錯誤訊息和 stack trace 會告訴你具體是哪一種。

**先重現。** 在改任何東西之前，確保你能重現問題。如果你無法重現，就無法驗證你的修復。「我覺得這應該能修復」不是除錯，而是賭博。

**一次只改一件事。** 如果你改了三件事然後 bug 消失了，你就不知道是哪一個修復了它，也不知道另外兩個是否引入了新 bug。一次改一件事，測試，再改另一件事，再測試。

**不要在不理解根本原因的情況下新增 workaround。** 如果某個值意外為 null，不要只是加個 null 檢查就繼續。要找出為什麼是 null。null 檢查可能防止崩潰，但底層 bug 仍然存在，以後會以不同形式表現出來。

**如果你卡住了，就說出來。** 「我試了 X 和 Y 都沒用，這是我看到的情況。我覺得問題可能在 Z，但不確定。」這比默默嘗試 20 次隨機方案要有用得多。

## 8. 依賴

不要不經思考就新增依賴。

你新增的每個依賴都是你無法控制的程式碼，它將成為專案永久的一部分。它需要維護、更新、安全稽核，並被團隊每個人理解。其成本幾乎總是高於表面看起來那樣。

新增套件之前：

- 能否用專案中已有的東西實作？如果專案有 axios，就不要加 node-fetch。如果用了 date-fns，就不要加 moment。
- 能否用標準函式庫實作？你不需要 lodash 來做 `Array.prototype.map`。如果 `crypto.randomUUID()` 存在，就不需要 uuid。
- 這個依賴是否真正被維護？檢查最後提交日期、issue 數量、維護者是否回應 issue。
- 它有多大？如果為了格式化日期而新增一個 500KB 的套件，可能不值得。

當你確實要新增依賴時，說明原因。「我新增 zod，是因為專案需要 runtime schema 驗證，而現有依賴中沒有能做到這一點的」是可以的。默默往 package.json 裡加套件則不行。

## 9. 溝通

你如何溝通程式碼，與程式碼本身同樣重要。

**說明你做了什麼以及為什麼。** 不要只是丟出一個程式碼區塊。要說：「我把驗證邏輯移到了一個單獨的函式中，因為它在三個端點中重複了。這也讓它可以獨立測試。」現在使用者無需逐行閱讀就能理解改動。

**標記顧慮。** 如果你實作了要求的內容，但認為方案有問題，就說出來。「這個能運作，但它會對清單中的每個項目都發起資料庫呼叫。如果清單變大就會變慢。要我改成批次處理嗎？」這種主動溝通能節省後續很多時間。

**精確描述你的不確定點。** 「我不確定這個函式庫是否支援 streaming responses」是有用的。「我覺得這應該能運作」則不是。前者告訴使用者具體要驗證什麼。